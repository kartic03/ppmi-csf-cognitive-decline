# Artifact integrity sweep

Every artifact named in the figure brief, tested against the committed anchors before any of it is plotted. Reports only -- nothing here was fixed or regenerated.

Generated by `revision/figures/artifact_integrity_sweep.py`. All deltas are computed at runtime from the artifacts themselves.

## Anchors

| anchor | field | value |
|---|---|---|
| `phase2/increment.json` `rq1_csf` | r2_base | 0.069214773536965479 |
| `phase2/increment.json` `rq1_csf` | r2_aug | 0.12734763266201798 |
| `phase2/increment.json` `rq1_csf` | dr2 | 0.058132859125052505 |
| `phase2/increment.json` `rq1_csf` | n | 607 |
| `mr/poscontrol_result.json` | deCODE OR | 1.491785 |
| `mr/poscontrol_result.json` | UKB-PPP OR | 0.994015 |

## Verdict key

| verdict | meaning |
|---|---|
| **ANCHOR** | this file defines the anchor |
| **PASS** | has an overlapping arm; agrees within 1e-6 |
| **ROUNDED** | has an overlapping arm stored at reduced precision; agrees at its stored precision, fails a strict 1e-6 assert |
| **FAIL** | has an overlapping arm; does not agree |
| **UNANCHORABLE** | no arm overlaps any committed value -- cannot be checked at all |

**Summary of 17 artifacts:** 1 ANCHOR, 1 FAIL, 7 PASS, 5 ROUNDED, 3 UNANCHORABLE.

## Per-artifact verdict

| artifact | anchorable? | verdict |
|---|---|---|
| `phase2/increment.json` | n/a (defines the anchor) | **ANCHOR** |
| `phase2/bootstrap_ci.json` | yes -- point_dr2 vs anchor dr2 | **PASS** |
| `phase2/noise_floor_distribution.json` | partly -- csf_increment is a 4 dp restatement of the anchor dr2 | **ROUNDED** |
| `phase2/enriched_baseline_check.json` | yes -- eb_slope|thin is the control arm | **FAIL** |
| `phase2/estimand_audit.json` | yes -- eb_slope|thin is the control arm | **PASS** |
| `phase2/phase1_visit_split.json` | yes -- results.eb_slope.full_sample is the pooled arm | **PASS** |
| `phase2/hlavnicka_head2head.json` | no | **UNANCHORABLE** |
| `phase2/iecv_site.json` | partly -- reference_random_5fold is a 4 dp restatement of the anchor | **ROUNDED** |
| `phase2/rank11_controls.json` | yes -- arms.anchor_csf is a deliberate anchor arm | **PASS** |
| `phase2/gba_scale_anchor.json` | partly -- csf_dr2 is a 4 dp restatement of the anchor dr2 | **ROUNDED** |
| `phase2/trial_enrichment_ci.json` | no | **UNANCHORABLE** |
| `phase2/calibration_flexible.json` | no against the committed anchor -- yes against two sibling artifacts | **ROUNDED** |
| `phase3/fairness_audit.json` | yes -- overall.r2 is the augmented (clinical+CSF) model R2 | **PASS** |
| `phase2/batch1_checks.json` | yes -- noise_block.r2_base vs anchor; age arm vs fairness_audit | **PASS** |
| `phase2/calibration.json` | partly -- calib_slope vs calibration_flexible overall_slope | **ROUNDED** |
| `mr/mr_power_bounds.json` | partly -- deCODE positive control only | **PASS** |
| `mr/mr_final_table.csv` | no against the MR anchor | **UNANCHORABLE** |

## Value-level checks

| artifact | field | compared against | expected | actual | delta | <=1e-6 | verdict |
|---|---|---|---|---|---|---|---|
| `bootstrap_ci.json` | `point_dr2` | dr2 | 0.05813285913 | 0.05813285913 | +0.000e+00 | yes | **PASS** |
| `noise_floor_distribution.json` | `csf_increment` | dr2 | 0.05813285913 | 0.0581 | -3.286e-05 | no | **ROUNDED** |
| `enriched_baseline_check.json` | `results[eb_slope|thin].r2_base` | r2_base | 0.06921477354 | 0.07022738337 | +1.013e-03 | no | **FAIL** |
| `enriched_baseline_check.json` | `results[eb_slope|thin].r2_aug` | r2_aug | 0.1273476327 | 0.1210231409 | -6.324e-03 | no | **FAIL** |
| `enriched_baseline_check.json` | `results[eb_slope|thin].dr2` | dr2 | 0.05813285913 | 0.05079575758 | -7.337e-03 | no | **FAIL** |
| `estimand_audit.json` | `results[eb_slope|thin].r2_base` | r2_base | 0.06921477354 | 0.06921477354 | +0.000e+00 | yes | **PASS** |
| `estimand_audit.json` | `results[eb_slope|thin].r2_aug` | r2_aug | 0.1273476327 | 0.1273476327 | +0.000e+00 | yes | **PASS** |
| `estimand_audit.json` | `results[eb_slope|thin].dr2` | dr2 | 0.05813285913 | 0.05813285913 | +0.000e+00 | yes | **PASS** |
| `phase1_visit_split.json` | `results.eb_slope.full_sample.r2_base` | r2_base | 0.06921477354 | 0.06921477354 | +0.000e+00 | yes | **PASS** |
| `phase1_visit_split.json` | `results.eb_slope.full_sample.r2_aug` | r2_aug | 0.1273476327 | 0.1273476327 | +0.000e+00 | yes | **PASS** |
| `phase1_visit_split.json` | `results.eb_slope.full_sample.dr2` | dr2 | 0.05813285913 | 0.05813285913 | +0.000e+00 | yes | **PASS** |
| `iecv_site.json` | `reference_random_5fold.r2_base` | r2_base | 0.06921477354 | 0.0692 | -1.477e-05 | no | **ROUNDED** |
| `iecv_site.json` | `reference_random_5fold.r2_aug` | r2_aug | 0.1273476327 | 0.1273 | -4.763e-05 | no | **ROUNDED** |
| `iecv_site.json` | `reference_random_5fold.dr2` | dr2 | 0.05813285913 | 0.0581 | -3.286e-05 | no | **ROUNDED** |
| `rank11_controls.json` | `arms.anchor_csf.r2_base` | r2_base | 0.06921477354 | 0.06921477354 | +0.000e+00 | yes | **PASS** |
| `rank11_controls.json` | `arms.anchor_csf.r2_aug` | r2_aug | 0.1273476327 | 0.1273476327 | +0.000e+00 | yes | **PASS** |
| `rank11_controls.json` | `arms.anchor_csf.dr2` | dr2 | 0.05813285913 | 0.05813285913 | +0.000e+00 | yes | **PASS** |
| `gba_scale_anchor.json` | `csf_dr2` | dr2 | 0.05813285913 | 0.0581 | -3.286e-05 | no | **ROUNDED** |
| `gba_scale_anchor.json` | `observed_dr2` | rank11_controls arms.pos_gba_status.dr2 | 0.0115884946 | 0.0116 | +1.151e-05 | no | **ROUNDED** |
| `calibration_flexible.json` | `overall_slope` | fairness_audit overall.cal_slope | 1.005804904 | 1.005804904 | +6.661e-16 | yes | **PASS** |
| `calibration_flexible.json` | `overall_slope` | calibration.json continuous.calib_slope | 1.0058 | 1.005804904 | +4.904e-06 | no | **ROUNDED** |
| `calibration_flexible.json` | `age_tertile_calibration[age<=58].slope` | fairness_audit age_tertile[age<=58].cal_slope | 0.2711370063 | 0.2711370063 | -3.886e-16 | yes | **PASS** |
| `calibration_flexible.json` | `age_tertile_calibration[age 58-67].slope` | fairness_audit age_tertile[age_58-67].cal_slope | 1.086185907 | 1.086185907 | -1.110e-15 | yes | **PASS** |
| `calibration_flexible.json` | `age_tertile_calibration[age>67].slope` | fairness_audit age_tertile[age>67].cal_slope | 1.118700957 | 1.118700957 | +8.882e-16 | yes | **PASS** |
| `fairness_audit.json` | `overall.r2` | r2_aug | 0.1273476327 | 0.1273476327 | +1.110e-16 | yes | **PASS** |
| `batch1_checks.json` | `noise_block.r2_base` | r2_base | 0.06921477354 | 0.06921477354 | +0.000e+00 | yes | **PASS** |
| `batch1_checks.json` | `age_tertile_dr2[age <= 58].r2_aug` | fairness_audit age_tertile[age<=58].r2 | -0.08591601568 | -0.08591601568 | +0.000e+00 | yes | **PASS** |
| `batch1_checks.json` | `age_tertile_dr2[age 58-67].r2_aug` | fairness_audit age_tertile[age_58-67].r2 | 0.1211289908 | 0.1211289908 | +1.110e-16 | yes | **PASS** |
| `batch1_checks.json` | `age_tertile_dr2[age > 67].r2_aug` | fairness_audit age_tertile[age>67].r2 | 0.09909154776 | 0.09909154776 | -3.331e-16 | yes | **PASS** |
| `calibration.json` | `continuous.calib_slope` | calibration_flexible overall_slope | 1.005804904 | 1.0058 | -4.904e-06 | no | **ROUNDED** |
| `mr_power_bounds.json` | `gpnmb_positive_control_or` | poscontrol deCODE OR | 1.491785 | 1.491785 | +0.000e+00 | yes | **PASS** |
| `mr_final_table.csv` | `PDGFRB.decode_or` | mr_power_bounds cells[PDGFRB|decode].or | 0.992242 | 0.992 | -2.420e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `PDGFRB.ukbppp_or` | mr_power_bounds cells[PDGFRB|ukbppp].or | 0.972713 | 0.973 | +2.870e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `ICAM1.decode_or` | mr_power_bounds cells[ICAM1|decode].or | 0.988426 | 0.988 | -4.260e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `ICAM1.ukbppp_or` | mr_power_bounds cells[ICAM1|ukbppp].or | 1.175851 | 1.176 | +1.490e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `VCAM1.ukbppp_or` | mr_power_bounds cells[VCAM1|ukbppp].or | 4.541083 | 4.541 | -8.300e-05 | no | **ROUNDED** |
| `mr_final_table.csv` | `MMP9.decode_or` | mr_power_bounds cells[MMP9|decode].or | 0.895606 | 0.896 | +3.940e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `MMP9.ukbppp_or` | mr_power_bounds cells[MMP9|ukbppp].or | 0.959497 | 0.959 | -4.970e-04 | no | **ROUNDED** |
| `mr_final_table.csv` | `MMP2.decode_or` | mr_power_bounds cells[MMP2|decode].or | 0.787086 | 0.787 | -8.600e-05 | no | **ROUNDED** |

## Notes per artifact

**`phase2/increment.json` -- ANCHOR**

rq1_csf is the committed headline all other R2 arms are tested against.

**`phase2/bootstrap_ci.json` -- PASS**

n=607 matches anchor n=607: True. ci_lo/ci_hi are the bootstrap resample spread and have no committed counterpart, so they are not independently checkable.

**`phase2/noise_floor_distribution.json` -- ROUNDED**

n_subjects=607 matches anchor n: True. The 20 dr2_draws are the null itself and have no committed counterpart -- the null distribution is UNANCHORABLE and rests on the correctness of its own run.

**`phase2/enriched_baseline_check.json` -- FAIL**

Declares the same n (607), the same cv_config and the same column lists as estimand_audit.json, yet disagrees on all four shared arms. Its control-arm dr2 (0.05079576) is exactly equal to increment.json rq1_csf.perm_observed_dr2_fixed, i.e. the fixed-alpha permutation estimator, not the tuned nested-CV estimator its cv_config declares. Mislabelled estimator, not seed noise. EXCLUDED from the figure set.

**`phase2/estimand_audit.json` -- PASS**

n=607 matches anchor. Reproduces the committed control arm to the last digit, so its enriched arm and its age/visit strata are used as the trusted source. NOTE: the lo/hi fields on its arms are cross-seed repeat ranges (dr2_repeat_lo/hi), NOT confidence intervals, and must never be drawn as error bars.

**`phase2/phase1_visit_split.json` -- PASS**

n_full=607 matches anchor. The six per-cell rows are scored on shared out-of-fold predictions (the file says so itself) and have no committed counterpart, so the CELLS are unanchorable even though the pooled arm passes.

**`phase2/hlavnicka_head2head.json` -- UNANCHORABLE**

Different endpoint (dichotomised conversion), different sample (n=324, 127 events) and a different metric (AUC). No arm overlaps the R2 anchor or the MR anchor. Nothing in this file can be checked against a committed value. Its own caveats state the brackets are cross-seed ranges, not CIs, and that the H4 arm is the most favourable of four fainting-item choices.

**`phase2/iecv_site.json` -- ROUNDED**

n_pooled=607 matches anchor. The reference arm agrees at 4 dp. The leave-one-site-out and random-cluster arms are the file's own results and have no committed counterpart -- UNANCHORABLE. Primary calibration is calibration_meta_real_sites_only (k=10); the k=11 figure includes the pooled 38-site cluster and must not be used.

**`phase2/rank11_controls.json` -- PASS**

anchor_csf n=607 matches anchor. The seven control arms are the file's own results and are unanchorable individually, but the file carries its anchor arm precisely, which is the strongest form of self-check in the set. Its repeat_lo/hi are cross-seed ranges, not CIs.

**`phase2/gba_scale_anchor.json` -- ROUNDED**

Scored on the full cohort (n=816), not the CSF-complete set, so its n does not match the anchor by design. Its observed_dr2 is a 4 dp restatement of rank11_controls pos_gba_status.dr2 (secondary cross-check, shown above). The prevalence ceiling (0.022708) is derived only within this file and is UNANCHORABLE.

**`phase2/trial_enrichment_ci.json` -- UNANCHORABLE**

Declares n=607 (matches the anchor cohort size) but reports percentage sample-size reductions, not R2. No arm overlaps any committed value. Its own csf_incremental medians straddle zero at every threshold (excludes_zero = [0.0]), which is the reportable finding.

**`phase2/calibration_flexible.json` -- ROUNDED**

Contains no R2 arm, so it cannot be tested against increment.json. Its overall_slope agrees with fairness_audit.json overall.cal_slope to 1e-6 and with calibration.json continuous.calib_slope at its stored 4 dp. The per-age-tertile slopes (including the youngest at 0.271137) reconcile with fairness_audit age_tertile -- cross-checked below.

**`phase3/fairness_audit.json` -- PASS**

n=607 matches anchor. overall.r2 reproduces the committed r2_aug, which anchors the whole subgroup table. Subgroup R2 and calibration slopes have no committed counterpart individually.

**`phase2/batch1_checks.json` -- PASS**

PARTIALLY SUPERSEDED, AND THE SCOPE MATTERS. noise_floor_distribution.json states it replaces the single-draw -0.0093 figure in this file -- that is `noise_block.dr2` (-0.00928308308798675), so the supersession lands on the noise_block section ONLY. It does not touch age_tertile_dr2. The age arm is the only source of age-stratified increments in data/processed and is used for Figure 2. It is corroborated laterally: all three per-tertile r2_aug reproduce fairness_audit.json's age_tertile r2 to ~1e-16 at identical n (203/202/202), and fairness_audit itself PASSES against the committed r2_aug. The file's own noise_block.r2_base also reproduces the committed anchor exactly. Do NOT use noise_block for the null -- use noise_floor_distribution.json's 20 draws.

**`phase2/calibration.json` -- ROUNDED**

THE ONLY SOURCE OF CALIBRATION CURVE COORDINATES IN THE PROJECT -- no other artifact in data/processed holds observed-vs-predicted points of any kind. Contains no R2 arm, so it cannot be tested against increment.json. continuous.n = 607 matches the anchor n, and its 10 decile bins sum to exactly 607, so the curve partitions the cohort. Its calib_slope agrees with calibration_flexible.json's overall_slope at its stored 4 dp (delta -4.904e-06). USED FOR FIGURE 3 PANEL A. The curve it provides is a BINNED DECILE curve; the 'flexible' calibration result exists only as flexible_mean_abs_deviation in calibration_flexible.json, with no coordinates anywhere in data/processed. The two must not be conflated and no smooth flexible fit is drawn in the figure set. Its `binary` section (n=548) is a different endpoint and is not used.

**`mr/mr_power_bounds.json` -- PASS**

Carries the deCODE positive-control OR exactly. It stores NO UKB-PPP positive-control field, so the UKB-PPP anchor (OR=0.994015) cannot be checked against this file -- consistent with the gate deviation recorded in poscontrol_result.json, where GPNMB recovers on deCODE but not on UKB-PPP. The eight per-protein cells are the file's own estimates and are unanchorable individually.

**`mr/mr_final_table.csv` -- UNANCHORABLE**

Contains no GPNMB row, so neither positive-control OR appears in it and it cannot be tested against poscontrol_result.json. Its 8 protein-by-platform ORs are 3 dp restatements of mr_power_bounds.json and agree at that precision (cross-check below), which makes the two MR files mutually consistent but jointly unanchored to the positive control.

## The one failure, in full

`phase2/enriched_baseline_check.json` and `phase2/estimand_audit.json` score the same four arms on the same declared cohort with the same declared cross-validation configuration and the same declared column lists. They disagree on all four.

| arm | field | estimand_audit | enriched_baseline_check | difference |
|---|---|---|---|---|
| `eb_slope|enriched` | r2_base | 0.21064665 | 0.21316584 | -0.00251918 |
| `eb_slope|enriched` | r2_aug | 0.25624884 | 0.25582393 | +0.00042491 |
| `eb_slope|enriched` | dr2 | 0.04560219 | 0.04265809 | +0.00294409 |
| `eb_slope|thin` | r2_base | 0.06921477 | 0.07022738 | -0.00101261 |
| `eb_slope|thin` | r2_aug | 0.12734763 | 0.12102314 | +0.00632449 |
| `eb_slope|thin` | dr2 | 0.05813286 | 0.05079576 | +0.00733710 |
| `slope_excl_baseline|enriched` | r2_base | 0.03504162 | 0.04304671 | -0.00800509 |
| `slope_excl_baseline|enriched` | r2_aug | 0.06203980 | 0.05381680 | +0.00822300 |
| `slope_excl_baseline|enriched` | dr2 | 0.02699819 | 0.01077009 | +0.01622809 |
| `slope_excl_baseline|thin` | r2_base | 0.02609730 | 0.02685014 | -0.00075283 |
| `slope_excl_baseline|thin` | r2_aug | 0.05245338 | 0.04590801 | +0.00654536 |
| `slope_excl_baseline|thin` | dr2 | 0.02635607 | 0.01905788 | +0.00729820 |

Declared `n` identical: True (607 vs 607). Declared cross-validation config identical: True. Thin column list identical: True. Enriched column list identical: True.

`enriched_baseline_check.json` control-arm dr2 = 0.05079576, which is exactly `increment.json rq1_csf.perm_observed_dr2_fixed` = 0.05079576. The disagreement is therefore an estimator substitution -- the fixed-alpha permutation estimator reported under a nested-CV label -- not run-to-run variation. It is one-directional: the file's dr2 is lower on all four arms.

## What this means for the figure set

1. `enriched_baseline_check.json` is excluded. Figure 1's enriched baseline is taken from `estimand_audit.json`, which reproduces the committed control arm exactly.
1b. `batch1_checks.json` was not in the original brief but became a figure source, since it is the only artifact holding age-tertile dR2. It is partially superseded and the SCOPE is section-level: `noise_floor_distribution.json` replaces its `noise_block` figure only, and leaves `age_tertile_dr2` untouched. The age arm is corroborated against `fairness_audit.json` to ~1e-16 at identical n. Figure 2 uses the age arm; the null everywhere comes from `noise_floor_distribution.json`.
2. Four artifacts carry a deliberate anchor arm and pass to 1e-6: `bootstrap_ci`, `estimand_audit`, `phase1_visit_split`, `rank11_controls`, plus `fairness_audit` on the augmented R2. These are the best-evidenced files in the set.
3. Three artifacts hold 4 dp restatements of the anchor and are consistent at that precision but cannot support a stricter claim: `noise_floor_distribution`, `iecv_site`, `gba_scale_anchor`.
4. Three artifacts are UNANCHORABLE outright -- `hlavnicka_head2head`, `trial_enrichment_ci`, `mr_final_table.csv`. They are not wrong; they are uncheckable. Nothing in the manuscript should lean on them the way it can lean on the anchored files, and the AUC head-to-head in particular carries the paper's negative result on the dichotomised endpoint.
5. The MR arm is anchored only on deCODE. The UKB-PPP positive control is absent from `mr_power_bounds.json` because GPNMB does not recover there -- so the UKB-PPP side of the PDGFRB null is ungated, exactly as `poscontrol_result.json` records. Figure 6 must mark it.
6. Two quantities the figures rely on have no committed counterpart anywhere: the 20-draw random-feature null and the per-site IECV results. Both rest entirely on the correctness of their own run.

